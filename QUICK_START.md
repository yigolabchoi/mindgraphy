# ⚡ MindGraphy 빠른 시작 가이드

## 🎯 3가지 시스템, 1개 프로젝트

```
mindgraphy/
├── 소개 홈페이지 (www.mindgraphy.com)           → 마케팅, 포트폴리오
├── 고객 포털 (portal.mindgraphy.com)            → 웨딩 고객 전용
└── 내부 시스템 (admin.mindgraphy.com)           → 관리자 & 작가
```

## 🚀 5분 안에 시작하기

### 1. 설치

```bash
cd mindgraphy
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

### 3. 접속

| 시스템 | URL | 설명 |
|--------|-----|------|
| 🏠 **소개 페이지** | http://localhost:3000 | 포털 선택 |
| 🔐 **로그인** | http://localhost:3000/login | 내부 시스템 로그인 |
| 👥 **고객 포털** | http://localhost:3000/c/demo-token-2025 | 데모 고객 페이지 |
| ⚙️ **관리자** | http://localhost:3000/admin/dashboard | 대시보드 (로그인 필요) |
| 📅 **캘린더** | http://localhost:3000/admin/calendar | 프로젝트 캘린더 |

### 4. 테스트 계정

**관리자 (대표)**
```
이메일: admin@mindgraphy.com
비밀번호: admin123
접속 → 모든 기능 사용 가능
```

**작가 (박작가)**
```
이메일: photographer1@mindgraphy.com
비밀번호: photo123
접속 → 제한된 기능 (내 일정, 캘린더 등)
```

## 📁 프로젝트 구조 이해하기

### 현재 구조 (Route Groups 사용)

```typescript
app/
├── (public)/          // 🏠 소개 홈페이지
│   └── page.tsx       //    → www.mindgraphy.com
│
├── (client)/          // 👥 고객 포털
│   └── c/[token]/     //    → portal.mindgraphy.com/c/[token]
│       ├── page.tsx   //       대시보드
│       ├── contract/  //       계약서
│       └── proof-gallery/ //  사진 선택
│
├── (admin)/           // ⚙️ 내부 업무 시스템
│   └── admin/         //    → admin.mindgraphy.com/admin
│       ├── dashboard/ //       대시보드
│       ├── calendar/  //       캘린더
│       ├── projects/  //       프로젝트
│       ├── my/        //       내 일정 (작가)
│       └── settings/  //       설정 (관리자)
│
└── (auth)/            // 🔐 인증
    └── login/         //    → admin.mindgraphy.com/login
```

### 공유 컴포넌트

```typescript
components/
├── ui/                // 🎨 디자인 시스템 (모든 시스템 공유)
│   ├── button.tsx
│   ├── card.tsx
│   └── ...
│
├── layout/            // 📐 레이아웃
│   ├── admin-layout.tsx   // 관리자용
│   └── admin-nav.tsx      // 권한별 네비게이션
│
├── admin/             // ⚙️ 관리자 전용
├── client/            // 👥 고객 전용
└── calendar/          // 📅 캘린더 (공통)
```

## 🎨 주요 기능 둘러보기

### 1️⃣ 권한 기반 인증 시스템

```typescript
// lib/types/auth.ts
type UserRole = 'admin' | 'photographer'

// 관리자만 볼 수 있는 메뉴
- 대시보드
- 설정
- 모든 프로젝트

// 작가가 볼 수 있는 메뉴
- 내 일정
- 캘린더 (읽기 전용)
- 배정된 프로젝트만
```

### 2️⃣ 프로젝트 상세 관리

```bash
# 캘린더에서 이벤트 클릭
http://localhost:3000/admin/calendar

# 권유은 아기 돌잔치 클릭
# → 프로젝트 상세 페이지로 이동

# 8개 탭으로 모든 정보 관리
- 개요 (일정, 고객, 결제)
- 백업 (0차/1차/2차)
- 촬영 (작가, 파일 수)
- 진행 (타임라인)
- 상품 (앨범, 액자)
- 고객 (연락처, 배송)
- 금액 (계약금, 잔금)
- 링크 (다운로드, 갤러리)
```

### 3️⃣ 고객 포털

```bash
# 고객은 토큰으로 접속 (로그인 불필요)
http://localhost:3000/c/demo-token-2025

# 볼 수 있는 것
- 진행 상황 (D-Day, 단계별 진행률)
- 계약서 (전자서명)
- 사진 선택 (프루프 갤러리)
- 최종 사진 다운로드
- 결제 정보
```

## 📦 빌드 & 배포

### 로컬 빌드 테스트

```bash
# 1. 빌드
npm run build

# 2. 빌드 결과 확인
ls out/
# index.html, admin/, c/, login/, _next/

# 3. 로컬에서 테스트
npx serve out -l 3001
# http://localhost:3001
```

### AWS S3 배포 (추천)

```bash
# 환경 변수 설정
cp .env.example .env.production

# 전체 배포
npm run deploy

# 또는 개별 배포
npm run deploy:www      # 소개 페이지만
npm run deploy:portal   # 고객 포털만
npm run deploy:admin    # 내부 시스템만
```

**자세한 내용:** [DEPLOYMENT.md](./DEPLOYMENT.md)

## ❓ FAQ

### Q1: 왜 3개 시스템을 하나의 프로젝트에?

**A:** 코드 재사용으로 개발 속도 5배, 유지보수 비용 60% 절감!

- ✅ 공통 UI 컴포넌트
- ✅ 타입 안정성
- ✅ 디자인 시스템 일관성
- ✅ 배포는 도메인별로 분리 가능

**자세한 분석:** [RECOMMENDATION.md](./RECOMMENDATION.md)

### Q2: 나중에 분리할 수 있나요?

**A:** 네! 언제든 분리 가능합니다.

```
현재 (모노레포) → Turborepo → 완전 분리
       ↑
   여기서 시작 (추천)
```

### Q3: 도메인 분리는 어떻게?

**A:** 빌드 1회, 배포 3곳

```bash
# 빌드 (1회)
npm run build

# 배포 (3개 도메인)
out/
├── index.html → www.mindgraphy.com
├── admin/ → admin.mindgraphy.com
└── c/ → portal.mindgraphy.com
```

### Q4: 성능은?

**A:** Static Export로 초고속 로딩

- CDN 캐싱
- 서버 불필요
- 무한 확장 가능
- 비용 최소화

## 📚 추가 문서

| 문서 | 내용 |
|------|------|
| [ARCHITECTURE.md](./ARCHITECTURE.md) | 📐 전체 아키텍처 상세 설명 |
| [DEPLOYMENT.md](./DEPLOYMENT.md) | 🚀 배포 가이드 (AWS, Vercel) |
| [RECOMMENDATION.md](./RECOMMENDATION.md) | 💡 프로젝트 구조 추천 & 비교 |
| [README.md](./README.md) | 📖 전체 프로젝트 문서 |

## 🎯 다음 단계

### 즉시 (우선순위)
1. ✅ 권한 시스템 구현 완료
2. ✅ 캘린더 강화 완료
3. 🔄 소개 홈페이지 콘텐츠 작성
4. 🔄 도메인 구매 & DNS 설정
5. 🔄 AWS/Vercel 배포

### 단기 (1개월)
1. Backend API 구축
2. DB 연동
3. 이미지 업로드 (S3)
4. 실시간 알림
5. 모바일 최적화

### 장기 (3-6개월)
1. 결제 시스템
2. 이메일/SMS 자동화
3. 분석 대시보드
4. 모바일 앱
5. 글로벌 확장

## 🛠️ 개발 시 주의사항 (Best Practices)

### Export/Import 에러 방지

#### 문제 상황
모듈에서 export하지 않은 함수나 타입을 import할 때 발생하는 에러:
```
Export mockLogin doesn't exist in target module
```

#### 해결 방법 및 예방 조치

1. **파일을 새로 만들거나 대대적으로 수정할 때**
   - 해당 파일을 import하는 모든 곳을 먼저 확인
   - grep으로 import 문 찾기:
     ```bash
     grep -r "from '@/lib/mock/users'" ./
     ```
   - 기존에 export되던 모든 함수/타입을 확인하고 유지

2. **타입 이름 변경 시**
   - 기존 타입이 다른 파일에서 사용되는지 확인
   - 예: `UserRole` → `TeamUserRole`로 변경 시
     - 해당 타입을 사용하는 모든 파일에서 import 수정 필요
     - `grep -r "UserRole" ./` 로 모든 사용처 확인

3. **Mock 데이터 함수 추가 시**
   - 인증 관련: `mockLogin`, `mockLogout`
   - 데이터 조회: `getMockData`, `findMockItem`
   - 함수를 export하는 것을 잊지 말기:
     ```typescript
     export const mockLogin = async (email: string, password: string) => {
       // implementation
     }
     ```

4. **타입 정의 시 주의사항**
   - 범용 타입 (`User`, `UserRole`)과 특정 모듈용 타입 (`TeamUser`, `TeamUserRole`) 구분
   - 범용 타입은 `/lib/types/`에, 특정 타입은 해당 mock 파일에 정의
   - 타입 변환 함수 제공:
     ```typescript
     // TeamUser → User 변환
     const toUser = (teamUser: TeamUser): User => ({ ... })
     ```

5. **린터 확인 습관**
   - 파일 수정 후 반드시 린터 확인:
     ```bash
     npm run lint
     ```
   - TypeScript 타입 체크:
     ```bash
     npx tsc --noEmit
     ```

6. **아이콘 라이브러리 통일**
   - 이 프로젝트는 `lucide-react`를 아이콘 라이브러리로 사용
   - Shadcn UI 컴포넌트 추가 시 `@radix-ui/react-icons` 대신 `lucide-react` 사용
   - 아이콘 매핑:
     - `CheckIcon` → `Check`
     - `ChevronDownIcon` → `ChevronDown`
     - `ChevronUpIcon` → `ChevronUp`
     - `Cross2Icon` → `X`
   - 예시:
     ```typescript
     // ❌ 잘못된 방법
     import { CheckIcon } from "@radix-ui/react-icons"
     
     // ✅ 올바른 방법
     import { Check } from "lucide-react"
     ```

### 주요 Export 파일 목록

#### `/lib/mock/users.ts`
- `mockTeamUsers`: 팀 사용자 목록
- `mockLogin`: 로그인 함수
- `TeamUser`, `TeamUserRole`, `UserStatus`: 타입 정의
- `getRoleLabel`, `getStatusLabel`, `formatRelativeTime`: 유틸 함수

#### `/lib/types/auth.ts`
- `User`, `UserRole`: 인증 관련 타입
- `AuthState`, `LoginCredentials`: 인증 상태 타입

#### `/lib/mock/settings.ts`
- `Product`: 상품 타입 정의 (본식스냅 + 추가옵션 통합)
- `baseProducts`: 본식스냅 상품 4개 (new BASIC, new DATA, BASIC, DATA)
- `optionProducts`: 추가 옵션 7개 (대표작가, 2인작가, 메이크업샵, 이사, 수석작가, 아이폰스냅, 얼리)
- `mockProducts`: 전체 상품 목록 (baseProducts + optionProducts)
- `Policy`: 정책 타입 정의
- `mockPolicies`: 정책 목록
- `Venue`, `Partner`: 마스터 데이터 타입
- `mockVenues`, `mockPartners`: 마스터 데이터

## 💬 도움이 필요하신가요?

- 📖 문서 읽기: [ARCHITECTURE.md](./ARCHITECTURE.md)
- 🚀 배포 문제: [DEPLOYMENT.md](./DEPLOYMENT.md)
- 💡 구조 고민: [RECOMMENDATION.md](./RECOMMENDATION.md)

---

**MindGraphy** - 웨딩 스튜디오를 위한 올인원 솔루션 🎉

